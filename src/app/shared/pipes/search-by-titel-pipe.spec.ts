import { SearchByTitelPipe } from './search-by-titel-pipe';

describe('SearchByTitelPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByTitelPipe();
    expect(pipe).toBeTruthy();
  });
});
